# Código-fonte

A implementação ainda não foi iniciada. Quando começar:

- Backend (Java/Spring): organizar em pacotes por camada (controller, service, repository, model), seguindo as entidades do `banco-de-dados/schema.sql`.
- Frontend (HTML/CSS/JS): uma pasta por tela ou por módulo, espelhando as telas descritas em `prototipos/`.
